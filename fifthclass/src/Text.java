/*
<What comes before the 'class' and 'method'>
(*)1. public: Can use anywhere
 2. protected: Same package + Another package's son's class
 3. default: Only in SAME package
 (*)4. private: Same package , Same file
-> protected, default : NOT frequently used
What comes BEFORE the 'class'
1. public: CAN IMPORT from other class.
2. default: CAN't IMPORT from other class.
*/
